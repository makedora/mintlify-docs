# Kernel — Browsers-as-a-Service API for Web Agents
# https://www.kernel.sh | Formerly OnKernel

## Documentation Pages

- /docs — Overview: what Kernel is, core capabilities, quick TypeScript example
- /docs/quickstart — Step-by-step: create account, set KERNEL_API_KEY, install @onkernel/sdk + playwright-core, create browser, connect via CDP
- /docs/browsers — Cloud browsers: kernel.browsers.create() returns cdp_ws_url, connectOverCDP() pattern, CDP retry logic, session timeouts (up to 72h)
- /docs/apps — Deploy apps: kernel CLI (create, deploy, invoke), playwright-core requirement, page.evaluate string literal gotcha
- /docs/troubleshooting — Error catalog: wrong package names, CDP race condition, page.evaluate bundler bug, 404 doc paths, correct URLs table
- /docs/api-reference — Full SDK reference: Kernel constructor, browsers.create() params (timeout, stealth), CLI commands, return types

## Quick Start

```bash
npm install @onkernel/sdk playwright-core
export KERNEL_API_KEY=your-key
```

```typescript
import Kernel from "@onkernel/sdk";
import { chromium } from "playwright-core";

const kernel = new Kernel();
const kb = await kernel.browsers.create();
const browser = await chromium.connectOverCDP(kb.cdp_ws_url);
const page = browser.contexts()[0].pages()[0];
await page.goto("https://example.com");
console.log(await page.title());
await browser.close();
```

## Key Details

- TypeScript SDK: `npm install @onkernel/sdk` (NOT `kernel` or `onkernel`)
- Python SDK: `uv pip install kernel`
- CLI: `brew install onkernel/tap/kernel`
- Auth: `KERNEL_API_KEY` environment variable
- Dashboard: https://dashboard.onkernel.com
- Use `playwright-core` not `playwright` for deployed apps
- Use `connectOverCDP()` not `connect()` — Kernel uses CDP
- page.evaluate: use string literals, not arrow functions (bundler bug)
- CDP retry: proxy may not be ready immediately after create(), retry with delay
