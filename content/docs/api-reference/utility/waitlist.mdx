---
title: Join Waitlist
description: Add an email to the waitlist
---

No authentication required.

<ParamField body="email" type="string" required>Email address to add.</ParamField>

<ResponseField name="success" type="boolean">`true` on success.</ResponseField>
<ResponseField name="priority" type="number">Queue position.</ResponseField>
<ResponseField name="referralLink" type="string">Unique referral link.</ResponseField>
