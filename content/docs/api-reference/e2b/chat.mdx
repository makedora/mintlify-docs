---
title: Chat
description: Stream a chat response from Claude inside the sandbox
---

Proxies to the sandbox's internal chat API (port 7031) and returns a Server-Sent Events stream.

<ParamField path="id" type="string" required>Sandbox ID.</ParamField>

### Response
Returns `text/event-stream` with Claude's response chunks.
